

pub fn ranged_ai_system() {
    //query melee dudes, have them move based on distance to nearest enemy. attack when in range. move back on a delay when enemy moves into a certain range. 
}

pub fn melee_ai_system() {
    //query melee dudes, have them move based on distance to nearest enemy. attack when in range 
}


pub fn monster_ai_system() {
    //similar to melee, but can pull out large gun and blast, or dive forwards, hitting all targets in the range. first major boss, later becomes a possible overrideable unit

}


pub fn mech_ai_system() {
    
}







#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
